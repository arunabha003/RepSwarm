# Required
CHAIN=sepolia
KEEPER_PRIVATE_KEY=0xYOUR_PRIVATE_KEY
FLASH_BACKRUNNER_ADDRESS=0xYourFlashLoanBackrunnerAddress

# One of these is required
# For local Anvil, set:
# SEPOLIA_RPC_URL=http://127.0.0.1:8545
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_KEY
SEPOLIA_WS_URL=wss://eth-sepolia.g.alchemy.com/v2/YOUR_KEY

# Optional tuning
POLL_INTERVAL_MS=2500
MAX_FLASHLOAN_AMOUNT_WEI=50000000000000000
MIN_PROFIT_WEI=0
DRY_RUN=false

# -----------------------------
# Optional: Hook-Agent Scoring (ERC-8004)
# -----------------------------
# This enables `npm run score`, which listens to AgentExecutor.AgentExecuted events
# and posts +1/-1 feedback to the official ERC-8004 ReputationRegistry.
#
# Required for scoring mode:
AGENT_EXECUTOR_ADDRESS=0xYourAgentExecutorAddress
ERC8004_REPUTATION_REGISTRY=0x8004B663056A597Dffe9eCcC1965A193B7388713

# Tags used for reputation entries (must match what you configure in AgentExecutor switching config)
SCORE_TAG1=swarm-hook
SCORE_TAG2=hook-agents

# Throttling (avoid spamming feedback on very busy pools)
MIN_FEEDBACK_INTERVAL_MS=5000
